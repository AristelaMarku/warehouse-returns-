# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=warehouse_returns
DB_USER=postgres
DB_PASSWORD=postgres

# Test database
DB_NAME_TEST=warehouse_returns_test

# JWT
JWT_SECRET=change-me-in-production-min-32-chars
JWT_EXPIRES_IN=8h

# App
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:4200

# Seed users (dev only — passwords used by seed script)
SEED_ADMIN_PASSWORD=admin123
SEED_SUPERVISOR_PASSWORD=super123
SEED_RECEIVER_PASSWORD=recv123
